Backend: server.js
- to run use npm start or npm test to start nodemon server
- change the isDev flag to true to enable logging and to use the local API key instead of the app engine key

Frontend: app.js
- change the isDev flag to true to change the baseURL to use localhost instead of deployed backend